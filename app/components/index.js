import {Size, CellSize, BoardWidth, BorderWidth, Color} from './GlobalStyle';

import Board from './Board/Board';
import Timer from './Timer/Timer';

export {Size, CellSize, BoardWidth, BorderWidth, Color, Board, Timer};
